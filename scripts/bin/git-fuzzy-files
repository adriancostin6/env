#!/usr/bin/env bash

main() {
	local repo_root="$(git rev-parse --show-toplevel)/"
	local files="$(git status --porcelain | fzf --multi | awk -v root="$repo_root" '{print root $2}')"
	echo "$files"
}

main
