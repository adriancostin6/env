# My configs

Mostly contains dotfiles, so things that should either go into the `$HOME` directory,
or `.$HOME/.config`.

Might also have some scripts that make your machine mine BITCOIN for me 🤖

# To install

Please don't 🥶

If you must, clone the repo and run `install.{sh,ps1}`